if which fortune >/dev/null 2>&1 && [ ! -f ~/.no_fortune_motd ]; then
	echo
	fortune -a | sed -e 's/^/    /'
	echo
fi

autoload -U compinit
compinit

export EDITOR=vim
export PAGER=less
export LC_CTYPE=en_US

alias l=ls
