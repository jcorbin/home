#!/bin/sh

cmd='less +f'
case $TERM in
	screen*)
		if [ -n "$STY" ]; then
			title='ltail'
			for arg in $@; do
				case "$arg" in
					-*)
						;;
					*)
						title="$title $arg"
						;;
				esac
			done
			title=${title#ltail }
			exec screen -t "$title" $cmd $@
		fi
		;;
esac

exec $cmd $@
