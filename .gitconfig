# Commiter identity
[user]
email=jcorbin@wunjo.org
name=Joshua T Corbin

[core]
# Use less as our pager, turn off the -c option if it was specified in $LESS,
# also don't use -S as per git default
pager="less -+c -FRX"

[color]
# Color all commands automatically
ui=auto

[apply]
# Fix all whitespace sins when applying patches
whitespace=fix

[branch]
# Track when branching and rebase by default
autosetupmerge=always
autosetuprebase=always

[pack]
# Use one thread per processor core for pac king
threads=0

# Command aliases
[alias]
st=status
br=branch
brav=branch -av
rebr=rewrite-branch
brb=branch-base
ci=commit
cim=commit -m
cic=commit -c
cia=commit -a
ciam=commit -a -m
co=checkout
coi=checkout-index
pack=repack -Ad
doh=commit --amend
cp=cherry-pick
rb=rebase
rbi=rebase --autosquash -i
rc=rebase --continue
ra=rebase --abort
rs=rebase --skip
sub=submodule
fp=format-patch -n -M -C --find-copies-harder
cn=clean -n
cf=clean -f
rem=remote
remv=remote -v
shbr=show-branch
ch=cherry -v
nrs=name-rev --stdin
dc=diff --cached
redo=reset HEAD^ --mixed
cfp=cat-file -p
rst=reset --hard
au=add --update
lg=log --graph --pretty=format:'%C(red)%h%Creset %C(green)%cd %Creset-%C(yellow)%d%Creset %s %C(bold blue)<%an>%Creset'
rlg=reflog --pretty=format:'%C(green)%gd %C(red)%h%Creset %Creset- %gs %C(bold blue)<%gn>%Creset'

[push]
	default = tracking
[log]
	date = relative
[diff]
	renames = copies
