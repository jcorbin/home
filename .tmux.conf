set -g utf8 on

set -g prefix C-u
unbind C-b
bind-key u send-prefix

set -sg escape-time 1
set -g repeat-time 1000
set -g default-terminal screen-256color
set -g default-shell /bin/zsh

bind-key C-r source-file ~/.tmux.conf \; display "Reloaded .tmux.conf"

unbind o
bind-key Tab last-pane #select-pane -t :.+
bind-key C-u last-window

# window splitting
unbind %
unbind '"'
bind-key | command-prompt -p command: "split-window -h '%%'"
bind-key - command-prompt -p command: "split-window -v '%%'"

# better new window
unbind-key c
bind-key c command-prompt -p command: "new-window '%%'"

unbind Down
unbind Up
unbind Left
unbind Right

bind-key j select-pane -D
bind-key k select-pane -U
bind-key h select-pane -L
bind-key l select-pane -R

bind-key -r J resize-pane -D
bind-key -r K resize-pane -U
bind-key -r H resize-pane -L
bind-key -r L resize-pane -R

unbind !
bind-key < break-pane
bind-key > command-prompt -p '[session:]window[.pane]' 'join-pane -s %%'

source-file "$HOME/.tmux-solarized.conf"
if-shell 'test -n "$POWERLINE_BINDINGS"' 'source-file "$POWERLINE_BINDINGS/tmux/powerline.conf"' 'source-file "$HOME/.tmux-old-statusline.conf"'

# vim:set ft=conf:
